# Feature Optimization

> This file tracks ways to REFINE features that already exist in this project.
> It is NOT a roadmap of new features to add — nothing here introduces new
> functionality. Every item makes an existing feature cleaner, faster, safer,
> more accessible, or otherwise better.
>
> Maintained by the `/optimize-features` command. Last full inventory: 2026-08-03

## Feature Inventory

### A. Host app — onboarding & reference

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 1 | Enable-keyboard walkthrough | Numbered 5-step guide plus troubleshooting that walks the user through registering the extension in iOS Settings; includes a deep link that opens Settings | `SleepTokenKB/EnableKeyboardView.swift` | 2026-07-26 |
| 2 | Alphabet chart | Scrollable 4-column reference grid of all 26 ritual glyphs with letter and prose description of each glyph's shape | `SleepTokenKB/AlphabetChartView.swift` | 2026-07-26 |
| 3 | Home hub & keyboard defaults | Serif hero with rune wordmark, destination cards, and settings controls (layout picker, 3-way key-face picker, haptics toggle with a permanent "Needs Full Access" caption, theme picker) that write shared prefs consumed by the extension | `SleepTokenKB/ContentView.swift` | 2026-07-30 |
| 18 | Theme system: Ritual & Even in Arcadia | Switchable app-wide aesthetic via `@Observable ThemeStore` (persisted): obsidian/gold vs pink-on-black with arcadian-green panels, drifting petal field, black-flamingo ornament, and a full-screen SLEEP TOKEN wordmark ceremony on switch (Reduce Motion gets a crossfade). Host app only; strictly cosmetic | `SleepTokenKB/Theme.swift`, `SleepTokenKB/ArcadiaRevealView.swift` | 2026-07-26 |
| 20 | Jerry hunt Easter egg | Ten near-invisible Jerrys (black body, white beak, pink legs) hidden across all four screens; each find pops a counter and persists. Finding all ten plays a one-time Damocles ceremony (petal storm, champagne wordmark) and opens the song on Apple Music. VoiceOver users get labels, announcements, and the full hunt | `SleepTokenKB/JerryHunt.swift`, `SleepTokenKB/Theme.swift` | 2026-07-26 |

### B. Rune Pad (host-app composer)

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 4 | Vertical rune composer | Type letters into vertical columns (space starts a new column); binary-searches the largest rune size that still fits the canvas so long text auto-shrinks. Letter pad is bottom-anchored like a real keyboard; the canvas absorbs all free height | `SleepTokenKB/RunePadView.swift` | 2026-07-26 |
| 5 | Multi-format export | Export menu: transparent PNG in light or dark ink, plaque PNG (self-backed, legible anywhere), system share sheet with a named PNG file, and plain-Latin-text copy — all rendered off-screen at export resolution | `SleepTokenKB/RunePadView.swift` | 2026-07-26 |
| 19 | Latin read-back with live spell check | READS strip translates the composed PUA runes back to Latin per keystroke; each word is checked with `UITextChecker` and unknown words are underlined in amber | `SleepTokenKB/RunePadView.swift`, `Shared/Alphabet.swift` | 2026-07-26 |

### C. Custom keyboard extension

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 6 | Letter input with QWERTY / A–Z grid layouts | The core typing surface; two selectable key arrangements that always insert plain Latin text via `UITextDocumentProxy` | `SleepTokenKeyboard/KeyboardRootView.swift`, `Shared/Alphabet.swift` | 2026-07-26 |
| 7 | Key face cycle (Rune / Rune·A / Aa) | One chrome key cycles three keycap faces: pure glyphs, glyphs with a small Latin hint beneath, plain ABC — with legacy hints-bool migration on read | `SleepTokenKeyboard/KeyboardRootView.swift`, `Shared/LayoutMode.swift` | 2026-07-26 |
| 8 | Shift and caps lock | `ShiftState` enum: single tap shifts one letter, second tap engages sticky caps lock, third releases; distinct SF Symbols and VoiceOver values per state | `Shared/ShiftState.swift`, `SleepTokenKeyboard/KeyboardRootView.swift` | 2026-07-26 |
| 9 | Numbers and punctuation page | A 30-key `123` page of digits and punctuation with its own backspace | `SleepTokenKeyboard/KeyboardRootView.swift` | 2026-07-26 |
| 10 | Keyboard chrome and adaptive height | Globe/space/return/layout/style bar, plus per-orientation and per-mode keyboard height driven through an explicit height constraint; return keycap relabels per host field via `textDidChange` | `SleepTokenKeyboard/KeyboardViewController.swift`, `SleepTokenKeyboard/KeyboardRootView.swift` | 2026-07-30 |
| 21 | Text-entry conveniences | Session of 2026-07-30: press-and-hold delete repeat with acceleration, autocapitalization honouring the host field's type without overriding manual shift/caps, double-space-for-period inside a 1s window, adaptive return-key titles, and haptics gated on `hasFullAccess` (the toggle was a silent no-op before `RequestsOpenAccess = true`). Each rule is a pure function; the views hold only wiring | `Shared/Autocapitalization.swift` + `AutoShift`/`SpaceTracker`, `Shared/KeyRepeat.swift` + `PeriodShortcut`/`ReturnKeyTitle`, `SleepTokenKeyboard/HostField.swift` | 2026-08-03 |
| 25 | Glide typing (slide-to-type) | QWERTY-only path-template decoding (SHARK² lineage): the trace is captured on the letter page with a reduced-motion-safe trail, decoded synchronously on lift against a bundled 50k-word frequency lexicon merged with the user's supplementary lexicon, and committed with stock casing, auto-space, one-shot whole-word undo, and up to three alternates in the bar; key centers derive from the same KeyboardMetrics arithmetic LetterPage lays out with, held together by a parity test; host-app toggle | `Shared/GlideDecoder.swift` + `Glide*`/`KeyCenters`, `Shared/Resources/glide_lexicon.txt`, `SleepTokenKeyboard/KeyboardRootView.swift` | 2026-08-03 |
| 26 | Suggestion bar with spelling correction | Candidate bar above the keys built on `UITextChecker` (iOS vends no autocorrect engine to extensions): up to two guesses per in-progress word with the literal always kept as the safety valve, boundary auto-replacement deliberately more conservative than the bar (min length 3, never digits, never a kept word), and a "keep as typed" that sticks for the session and after; a lone lowercase "i" capitalizes at word endings, the one exception to the length floor | `Shared/Suggestions.swift`, `SleepTokenKeyboard/KeyboardRootView.swift` | 2026-08-03 |
| 27 | Emoji page | Stock-style emoji page: category strip with Recents leading (clock-tab convention), recents persisted with a 24-item no-duplicate bound, and the layout/key-face pickers relocated behind a long press on the emoji key | `Shared/EmojiCatalog.swift`, `SleepTokenKeyboard/KeyboardRootView.swift` | 2026-08-03 |
| 28 | Stock-parity page and shift gestures | `PageAfterSpace`: space returns the 123/#+= detour to letters once the visit is armed by a keystroke, measured against the system keyboard on iOS 26; `CapsLockTap`: a quick second shift tap jumps straight to caps lock while the slow three-tap cycle survives — both windows take time as a parameter so they are testable | `Shared/PageAfterSpace.swift`, `Shared/CapsLockTap.swift`, `SleepTokenKeyboard/KeyboardRootView.swift` | 2026-08-03 |

### D. Shared core

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 11 | Ritual alphabet model and PUA rune mapping | `SleepTokenLetter` enum: Latin value, asset name, prose glyph description, and bidirectional mapping to Private Use Area codepoints U+E900–U+E919 | `Shared/Alphabet.swift` | 2026-07-26 |
| 12 | Glyph renderer with geometric fallback | Draws a letter from the asset catalog when present, otherwise falls back to a hand-coded Canvas approximation so the keyboard is never blank | `Shared/SymbolGlyphView.swift` | 2026-07-26 |
| 13 | Rune font loading and registration | Locates `SleepTokenRunes.ttf` in the bundle, registers it once per process, and optionally installs it system-wide via CoreText | `Shared/RuneFont.swift` | 2026-07-26 |
| 14 | Cross-process preference sync | App Group `UserDefaults` bridge so host-app settings reach the keyboard extension, with typed accessors and defaults | `Shared/LayoutMode.swift` | 2026-07-26 |

### E. Build, assets and ops

| # | Feature | What it does | Where it lives | Last reviewed |
|---|---------|--------------|----------------|---------------|
| 15 | xcodegen project generation | Single `project.yml` generates both targets, the test bundle, Info.plists, entitlements and the shared scheme | `project.yml` | 2026-07-26 |
| 16 | SVG to PDF/TTF asset pipeline | 26 source SVGs converted to template PDFs for the asset catalogs and expanded stroke-to-fill into a PUA-mapped TrueType font | `scripts/build_rune_font.py`, `Assets/Symbols/README.md`, `stkb-runes-svg/` | 2026-07-26 |
| 17 | Unit test suite | 252 pure-logic and bundle-level tests across thirty-three suites: rune round-tripping, layouts, shift-state machine, metrics, theme persistence, autocapitalization, key repeat, period shortcut, return-key titles, rune-font glyph integrity (26 distinct non-notdef PUA glyphs), and export-style exhaustiveness — with save/restore isolation | `SleepTokenKBTests/`, `scripts/test.sh` | 2026-07-30 |
| 22 | Release pipeline & ASC status probe | `release.sh` stages preflight/status/archive/validate/upload: cloud-managed signing via API key, `.ipa` verification (embedded appex, privacy manifests, build number), and a `status` stage that signs its own ES256 JWT with openssl/jq to report both Apple reviews, builds, beta groups, and App Store submission blockers | `scripts/release.sh`, `docs/RELEASE.md`, `.github/workflows/release.yml` | 2026-07-30 |
| 23 | CI build & test workflow | Push/PR workflow that generates the project with xcodegen and runs the full simulator test suite; release workflow is manual-dispatch only with `archive` as the safe default and base64 validation of the `.p8` secret | `.github/workflows/swift.yml`, `.github/workflows/release.yml` | 2026-07-30 |
| 24 | OG image generation | Three Python scripts render the repository's Open Graph art (device mockups, editorial card, glyph tile) into `docs/og-*.png` | `scripts/og/` | 2026-07-30 |

## Optimization Opportunities

_Run 2026-07-26 (host app, categories A & B — features 1, 2, 3, 4, 5, 18, 19): 7 read-only lens reviewers + 7 adversarial verifiers via workflow. 36 findings, 0 refuted, 8 impact ratings revised downward on verification. All 36 retained below with verified impact, ranked by impact within each feature. Unchecked = not yet implemented._

_All 32 refinements for features 6-10 below were implemented on 2026-07-26 (see `Implementation notes` at the end for what was adapted and why). Analysis run 2026-07-26 — keyboard extension core (features 6-10), 8 lenses. 63 raw findings, 3 refuted by adversarial verification, 56 retained, then merged across lenses into the 32 distinct refinements below (several lenses independently flagged the same lines). Ranked by impact, never by effort._

_Compile-verified 2026-07-26: `./scripts/test.sh` builds both targets against the iOS Simulator 26.5 SDK and runs 33 tests, all passing, with zero source warnings. Behaviour on a physical device is still unverified._

_Run 2026-07-31 (categories A–C, features 1–10, 18–21): 8 read-only lens reviewers + 8 adversarial verifiers via workflow, instructed to skip everything already recorded here. 41 raw findings, 41 confirmed, 0 refuted, deduped and merged by root cause into the 27 new items below (marked "added 2026-07-31"). 29 of 41 landed on feature 21 — the code written 2026-07-30 that no pass had ever reviewed. Features 2, 5, 6, 8, 9, 18, 19, 20 produced zero new findings._

_Run 2026-08-03 (features 25–28 + a narrow feature-21 re-review — the eleven Shared/ files and their KeyboardRootView wiring added since the 2026-07-31 pass): 8 read-only lens reviewers + 8 adversarial verifiers via workflow, instructed to skip everything already recorded here. 50 raw findings, 0 refuted, 4 impact ratings revised on verification (all four downward), merged by root cause into the 37 distinct items below (the emoji key's double haptic was found independently by four lenses; the CapsLockTap consecutiveness gap and the replaceCurrentWord bypass by two each)._

### 1. Enable-keyboard walkthrough

_Current state: Visually cohesive with the ritual-card language; the five-step structure reads clearly, the copy is honest about Full Access, and the troubleshooting card plus Settings deep link cover the real first-run friction._

- [x] **[UX]** Deep link lands on a screen the steps never describe — the gold button opens the app's own Settings page (which has a direct Keyboards list), but steps II–III only describe the manual General → Keyboard route, and step II's detail repeats its own title. Copy-only fix. Impact: High. Effort: Low. (`SleepTokenKB/EnableKeyboardView.swift:120-122`) — added 2026-07-26, completed 2026-07-26
- [x] **[Accessibility]** Step rows are fragmented for VoiceOver — numeral/title/detail are three separate elements, so users hear a bare "IV" with no sequence context; combine + label per the AlphabetChartView convention. Impact: Medium. Effort: Low. (`SleepTokenKB/EnableKeyboardView.swift:82-105`) — added 2026-07-26, completed 2026-07-26
- [x] **[Cleaner code]** Manually numbered steps subscript a fixed numeral array — five hand-maintained indices into `numerals`; a miscount traps at runtime. Model steps as data and derive numerals from the loop index. Impact: Low. Effort: Low. (`SleepTokenKB/EnableKeyboardView.swift:7`) — added 2026-07-26, completed 2026-07-26
- [x] **[Consistency]** Steps card is the app's only unlabeled ritual card group — troubleshooting and every ContentView group get a SectionLabel; the steps card does not. Impact: Low. Effort: Low. (`SleepTokenKB/EnableKeyboardView.swift:17-63`) — added 2026-07-26, completed 2026-07-26
- [ ] **[UX]** Simulator-only Command-K bullet ships to end users — wrap in `#if targetEnvironment(simulator)`. Impact: Low. Effort: Low. (`SleepTokenKB/EnableKeyboardView.swift:70`) — added 2026-07-26
- [x] **[UX]** The Full Access step explains why but never says where — step IV describes purpose and scope but never names the "Allow Full Access" switch or notes it sits on the same Settings screen step III already opened; the home-screen caption ("Needs Full Access, in Settings") gives no route either. Copy-only fix in both places. Impact: Medium. Effort: Low. (`SleepTokenKB/EnableKeyboardView.swift:26-29`, `SleepTokenKB/ContentView.swift:393-395`) — added 2026-07-31, completed 2026-07-31

### 2. Alphabet chart

_Current state: Tidy 70-line file, closely on-theme, glyph rendering statically cached, every card a combined accessibility element with an explicit label._

- [x] **[Accessibility]** VoiceOver reads each glyph description twice per card — the combined element inherits SymbolGlyphView's hint (the same description as the explicit label); hide the decorative glyph like RunePadView does. Impact: Medium. Effort: Low. (`SleepTokenKB/AlphabetChartView.swift:59-60`, `Shared/SymbolGlyphView.swift:43`) — added 2026-07-26, completed 2026-07-26
- [x] **[UX]** Descriptions truncate at large Dynamic Type in the fixed 3-column grid — `.caption2` + `lineLimit(3)` in a ~100pt column ellipsizes at xxxLarge and widely at accessibility sizes; drop the line limit and fall to 2 columns at accessibility sizes. Impact: Medium (downgraded from High — truncation starts later than filed). Effort: Low. (`SleepTokenKB/AlphabetChartView.swift:4,41-46`) — added 2026-07-26, completed 2026-07-26
- [x] **[Consistency]** GlyphCard hand-rolls the card chrome at radius 14 — exact duplicate of RitualCard's body except the app-standard 16pt radius; replace with `.ritualCard(padding: 0)`. Impact: Low. Effort: Low. (`SleepTokenKB/AlphabetChartView.swift:51-58`) — added 2026-07-26, completed 2026-07-26

### 3. Home hub & keyboard defaults

_Current state: Ceremony wiring is sound (pendingMode/guard sequencing, reduce-motion fallback), DefaultsRow unifies the card, persistence is robust and tested._

- [x] **[Resilience & tests]** Defaults card goes stale when the keyboard extension changes preferences — pickers seed once and re-read only on `.onAppear`, which does not fire on foregrounding; refresh all three controls on `scenePhase == .active`. Impact: Medium. Effort: Low. (`SleepTokenKB/ContentView.swift:265,279,212-217`) — added 2026-07-26, completed 2026-07-26
- [x] **[Accessibility]** Segmented pickers voice chrome-key abbreviations ("Rune·A", "Aa") — the model's full accessibility labels exist and the keyboard itself uses them; the home screen does not. Impact: Medium. Effort: Low. (`SleepTokenKB/ContentView.swift:271,290`, `Shared/LayoutMode.swift:25-30`) — added 2026-07-26, completed 2026-07-26
- [x] **[Consistency]** Three controls, three preference-sync patterns — LayoutPicker/KeyFacePicker duplicate a seed/onChange/onAppear dance while haptics uses a raw computed Binding; extract one preference-backed control helper. Impact: Low. Effort: Low. (`SleepTokenKB/ContentView.swift:264-308,212-217`) — added 2026-07-26, completed 2026-07-26
- [x] **[UI]** Top scrim is a fixed 110pt ramp regardless of the device's safe-area inset — on small-inset devices it dims resting content; derive height from the actual inset. Impact: Low. Effort: Low. (`SleepTokenKB/ContentView.swift:98-107`) — added 2026-07-26, completed 2026-07-26
- [x] **[Accessibility]** "Needs Full Access" caption uses ornament-tier ink at ~3.3:1 contrast — the one line explaining why haptics may do nothing is set in `Theme.inkFaint` (documented as "ornament and legal copy"), below the 4.5:1 AA minimum for caption-size text in both themes; `Theme.inkDim` (~6.3:1) is the established tier for supporting copy. One-word change. Impact: Medium. Effort: Low. (`SleepTokenKB/ContentView.swift:394-396`, `SleepTokenKB/Theme.swift:114-122`) — added 2026-07-31, completed 2026-07-31

### 4. Vertical rune composer

_Current state: Binary-search fit keeps the plaque in view, one shared columns view renders screen and export so they cannot drift visually, the empty state teaches the interaction, pad keys carry correct accessibility labels._

- [x] **[Resilience & tests]** Fit algorithm omits the trailing caret's height — `fits()` never counts the caret child + gap (~0.77 × runeSize) the screen appends to the last column, so tight fits overflow the plaque; CanvasMetrics has zero tests. Impact: Medium. Effort: Low. (`SleepTokenKB/RunePadView.swift:532-535,569-572`) — added 2026-07-26, completed 2026-07-26
- [x] **[Accessibility]** VoiceOver reads the canvas letter-by-letter with shape-description hints — collapse it to one element whose value is the latin words already computed. Impact: Medium (downgraded from High — the translation strip already speaks the words). Effort: Low. (`SleepTokenKB/RunePadView.swift:62-72`, `Shared/SymbolGlyphView.swift:42-43`) — added 2026-07-26, completed 2026-07-26
- [x] **[Cleaner code]** Canvas geometry constants duplicated between fit math and rendering view — `cell`, column width, chrome padding each derived twice; the caret bug is the drift this enables. Single-source them in CanvasMetrics (the KeyboardMetrics precedent). Impact: Medium. Effort: Low. (`SleepTokenKB/RunePadView.swift:526,551,527-530,575-576`) — added 2026-07-26, completed 2026-07-26
- [x] **[UI]** When even the 8pt minimum cannot fit, content draws past the plaque card — nothing clips the canvas; add `.clipShape` on the card shape so overflow degrades inside the plaque. Impact: Medium. Effort: Low. (`SleepTokenKB/RunePadView.swift:488,62-75`) — added 2026-07-26, completed 2026-07-26
- [x] **[UX]** Clear destroys the whole composition in one unconfirmed tap beside the Export menu — add `role: .destructive` + confirmationDialog. Impact: Medium. Effort: Low. (`SleepTokenKB/RunePadView.swift:37-38`) — added 2026-07-26, completed 2026-07-26
- [x] **[Architecture]** RunePadView.swift hosts three features across 620 lines and locks the pure fit math behind private scope — split canvas types and export types into their own files at internal access so tests can reach them. Impact: Medium. Effort: Medium. (`SleepTokenKB/RunePadView.swift:10,454,543`) — added 2026-07-26, completed 2026-07-26
- [x] **[UX]** Delete has no press-and-hold auto-repeat — one tap per rune to trim long text; add long-press repeat per platform convention. Impact: Low (downgraded from Medium). Effort: Medium. (`SleepTokenKB/RunePadView.swift:237-251`) — added 2026-07-26, completed 2026-07-26
- [x] **[UX + Resilience & tests + Performance]** July's delete-repeat overshoots by one, has no cancellation cleanup, and now diverges from Shared/KeyRepeat — the drag starts the repeater but the underlying Button still fires on touch-up, deleting one extra rune at release; the repeat Task is cancelled only in `onEnded` (no `onDisappear` in the file, and system touch-cancellation strands it at 12.5Hz); and its literal 0.45s/0.08s curve now disagrees with the tested `Shared/KeyRepeat` schedule the keyboard's delete uses. Restructure like `RepeatingIconKeyButton` and drive timing from KeyRepeat. Impact: Medium. Effort: Low. (`SleepTokenKB/RunePadView.swift:297,314-318,375-384`, `Shared/KeyRepeat.swift:12-28`) — added 2026-07-31, completed 2026-07-31

### 5. Multi-format export

_Current state: Three deliberate image styles centralized on RuneExportStyle with per-destination status copy, export-resolution metrics, displayScale-aware rendering, failure status on every path; security posture fine as-is._

- [x] **[Resilience & tests]** Redundant `UIPasteboard.image` assignment clobbers the PNG-first pasteboard item — line 330 replaces the `setItems` payload written at 326, discarding the explicit `public.png` representation that is the point of the transparent styles; delete the line (the else branch remains the fallback). Impact: High. Effort: Low. (`SleepTokenKB/RunePadView.swift:326,330`) — added 2026-07-26, completed 2026-07-26
- [x] **[UX]** Status capsule never fades, never expires, and is silent to VoiceOver — no `withAnimation` despite the declared transition, no expiry, no announcement; route status through one helper that animates, auto-clears, and announces. Impact: Medium. Effort: Low. (`SleepTokenKB/RunePadView.swift:82,316,334`) — added 2026-07-26, completed 2026-07-26
- [x] **[Resilience & tests]** Share flow observes no completion — temp PNG lingers, success is never confirmed, write is non-atomic; wire `completionWithItemsHandler` to clean up and confirm. Impact: Low. Effort: Low. (`SleepTokenKB/RunePadView.swift:344-349,445`) — added 2026-07-26, completed 2026-07-26
- [ ] **[Performance]** Unexplained fixed 50 ms sleep in renderImage plus main-actor PNG encoding — everything feeding the renderer is synchronous; replace with a commented yield or delete, move `pngData()` off the main actor. Note 2026-07-26: the sleep was replaced with a commented `Task.yield()`; the PNG encode deliberately stays on the main actor (occasional exports, ImageRenderer is main-bound anyway). Impact: Low (downgraded from Medium). Effort: Low. (`SleepTokenKB/RunePadView.swift:403,325,340`) — added 2026-07-26
- [x] **[Cleaner code]** Export menu metadata split between the view and RuneExportStyle — titles/symbols hand-written in three Button blocks while the enum owns ink and copy; make the enum CaseIterable with menuTitle/systemImage. Impact: Low. Effort: Low. (`SleepTokenKB/RunePadView.swift:265-281,428`) — added 2026-07-26, completed 2026-07-26

### 6. Letter input with QWERTY / A-Z grid layouts

_Current state: The layout data is genuinely clean: KeyboardLayout.qwertyRows and gridRows are shared, public, static tables that each contain all 26 letters exactly once, and both layouts render through one LetterPage rather than being forked into two views. Case is resolved in exactly one place (insertLetter at :118-124), so the input path itself is small and correct._

- [x] **[Accessibility]** No key carries the .isKeyboardKey trait, so VoiceOver cannot touch-type on this keyboard — Every key in the extension is a plain SwiftUI Button carrying at most an .accessibilityLabel. Impact: High. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:291-292`) — added 2026-07-26, completed 2026-07-26
- [x] **[UI]** Hardcoded 12pt home-row inset makes key width differ per row, and the error grows with screen width — Row widths are produced by dividing whatever space is left among the keys, and the QWERTY home row is nudged in by a flat 12pt that is not the half-key offset a QWERTY needs. Impact: Medium. Effort: Medium. (`SleepTokenKeyboard/KeyboardRootView.swift:198`) — added 2026-07-26, completed 2026-07-26
- [x] **[Performance & Memory + Resilience & Tests]** Every keystroke constructs two UserDefaults suites before the character is inserted — KeyboardPreferences.defaults is a computed property that builds a brand-new `UserDefaults(suiteName:)` on every access. Impact: Medium. Effort: Low. (`Shared/LayoutMode.swift:62-64`) — added 2026-07-26, completed 2026-07-26
- [x] **[Performance & Memory]** The haptic generator is re-allocated with every root view, so the prepare() warm-up is thrown away — `impact` is a stored property of the KeyboardRootView struct, so a new UIImpactFeedbackGenerator is constructed inside every KeyboardRootView(...) — once in installKeyboardUI and once per... Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:19`) — added 2026-07-26, completed 2026-07-26
- [x] **[UX]** Keypress acknowledgement fades in over 80ms, and the haptic meant to back it up may be inert without Full Access — The only per-keystroke feedback is KeyPressStyle, whose opacity and scale changes are wrapped in one `.easeOut(duration: 0.08)` animated on isPressed — applied to press AND release, so the... Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:364-370`) — added 2026-07-26, completed 2026-07-26
- [x] **[Resilience & Tests + Architecture]** The text document proxy is snapshotted into the view, while every other host interaction goes through a closure — KeyboardRootView stores `let proxy: UITextDocumentProxy` and calls insertText/deleteBackward on it from inside view code, while every other controller interaction in the same struct... Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:7-8`) — added 2026-07-26, completed 2026-07-26
- [x] **[Cleaner code]** Every preference is read in two places — the @State initialisers and onAppear — and neither is identifiable as authoritative — layoutMode, keyFaceStyle and showLatinHints are each read from KeyboardPreferences in their @State declarations and then read again and re-assigned in onAppear. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:12-13`) — added 2026-07-26, completed 2026-07-26
- [x] **[Cleaner code]** Four key structs re-implement the same keycap chassis; Shift and Backspace differ only in four literals — LetterKeyButton, ShiftKeyButton, BackspaceKeyButton and KeyChromeButton each independently build `RoundedRectangle(cornerRadius: 6, style: .continuous)` as a background and each... Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:286`) — added 2026-07-26, completed 2026-07-26
- [x] **[Resilience & Tests]** Shift and backspace placement is hardcoded to row index 2 of the QWERTY rows — On the letter page the shift and backspace keys are attached to `index == 2` and the middle-row inset to `index == 1`. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:178`) — added 2026-07-26, completed 2026-07-26
- [x] **[Resilience & Tests]** Neither keyboard layout's rows are validated for completeness or uniqueness — KeyboardLayout.qwertyRows and gridRows are hand-written literals that must together contain all 26 SleepTokenLetter cases exactly once, or a letter becomes untypeable in that layout. Impact: Low. Effort: Low. (`Shared/Alphabet.swift:76-80`) — added 2026-07-26, completed 2026-07-26

### 7. Key face style (rune art <-> ABC keycaps) + optional Latin hints under glyphs

_Current state: The style model is well-factored: one KeyFaceStyle enum with a `next` cycle, a single LetterKeyButton switch that renders both faces, and correct composition with the hints preference — line 187 suppresses hints in ABC mode so the two features never contradict each other. SymbolGlyphView also degrades gracefully to a geometric fallback if an asset is ever missing._

- [x] **[UI + Accessibility]** Dark mode inverts the key hierarchy: letter keycaps render darker than the keyboard behind them — Letter keys fill with `.systemBackground` while the keyboard field is `.secondarySystemBackground`. Impact: High. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:287`) — added 2026-07-26, completed 2026-07-26
- [x] **[Accessibility]** SymbolGlyphView's own label and hint nest inside LetterKeyButton's Button, so identical keys announce differently per style — SymbolGlyphView unconditionally attaches `.accessibilityLabel(Text(letter.upperLatin))` and `.accessibilityHint(Text(letter.glyphDescription))` to itself. Impact: Medium. Effort: Low. (`Shared/SymbolGlyphView.swift:29-30`) — added 2026-07-26, completed 2026-07-26
- [x] **[Accessibility]** Dynamic Type is honoured only by the three modifier SF Symbols, which are exactly the ones inside fixed-height frames — Every text element on a key face uses a hard-coded point size — the Latin hint is `size: 9`, the ABC keycap `max(17, keyHeight * 0.42)`, chrome and all 30 symbol keys 11/12/15. Impact: Medium. Effort: Medium. (`SleepTokenKeyboard/KeyboardRootView.swift:269`) — added 2026-07-26, completed 2026-07-26
- [x] **[Architecture + Resilience & Tests]** refreshRoot() cannot refresh the preference state it appears to refresh; prefs are mirrored in four places — refreshRoot() assigns a freshly built KeyboardRootView to hostingController.rootView, but SwiftUI keeps existing @State for a view of the same type at the same position, so the initializer... Impact: Medium. Effort: Medium. (`SleepTokenKeyboard/KeyboardViewController.swift:82`) — added 2026-07-26, completed 2026-07-26
- [x] **[Security & Privacy]** The extension writes to the App Group container, a write its no-open-access sandbox denies, and the `?? .standard` fallback masks it — The layout and key-style chrome buttons persist by writing to the App Group suite, landing on `defaults.set(...)` against `UserDefaults(suiteName: "group.ai.altivum.SleepTokenFanKB")`. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/Info.plist:33-34`) — added 2026-07-26, completed 2026-07-26
- [x] **[Performance & Memory]** SymbolGlyphView probes the asset catalog with UIImage(named:) inside its body, 26 times per render — SymbolGlyphView.body decides between asset and fallback by calling `UIImage(named: letter.assetName)` and discarding the result — it is used only as a nil test — then resolves the same... Impact: Low. Effort: Low. (`Shared/SymbolGlyphView.swift:16`) — added 2026-07-26, completed 2026-07-26
- [x] **[Cleaner code]** GeometricSymbol is unreachable in the extension, documented as a placeholder for art that has shipped, and builds a dead diamond Path on every fill — The MARK and doc comment describe GeometricSymbol as a stand-in 'until PDF assets are dropped in' so the keyboard is usable 'without hand-traced art' — but all 26 symbol_*.imageset PDFs... Impact: Low. Effort: Low. (`Shared/SymbolGlyphView.swift:34`) — added 2026-07-26, completed 2026-07-26
- [x] **[Cleaner code]** LetterKeyButton passes an argument that is already the default and stacks three frames on one branch only — The rune branch passes `foreground: .primary` to SymbolGlyphView, which already defaults that parameter to .primary. Impact: Low. Effort: Low. (`Shared/SymbolGlyphView.swift:9`) — added 2026-07-26, completed 2026-07-26
- [x] **[UI]** Rune keycap content sits 2pt above optical centre while the ABC keycap is centred, and the hint is a fixed 9pt literal — The two key faces are laid out by different mechanisms and do not balance. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:263-265`) — added 2026-07-26, completed 2026-07-26
- [x] **[UX]** ABC and hint key faces never reflect shift state — `letter.upperLatin` renders unconditionally on both faces and the hand-written `==` excludes shift, so keycaps are permanently uppercase; with autocapitalization now silently arming and releasing shift, the only case cue is the small shift icon. Pass `shift.isUppercase` into `LetterKeyButton`, render case accordingly, include it in `==`. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:456,463-465,410-428`) — added 2026-07-31, completed 2026-07-31

### 8. Shift and caps lock (tap = shift one letter, second tap = sticky caps lock)

_Current state: The three-state cycle is functionally correct — every transition was traced (off -> shift -> caps -> off) and the one-shot consumption at :121 correctly leaves caps lock sticky while clearing a single shift. Case resolution is centralised in insertLetter, so letter keys never need to know about shift at all._

- [x] **[Accessibility + UX]** The three-state shift machine is flattened to a Bool before it reaches the key, so caps lock is indistinguishable from one-shot shift — toggleShift maintains a genuine three-state machine, but LetterPage is handed the already-collapsed `isShifted || isCapsLock` and ShiftKeyButton only accepts `let isActive: Bool`. Impact: High. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:35`) — added 2026-07-26, completed 2026-07-26
- [x] **[Cleaner code + Architecture + Resilience & Tests]** Three shift states are encoded in two overlapping Bools, re-derived at two call sites, and no test target can reach them — isShifted and isCapsLock together encode off/shift/caps, with caps lock stored as both-true, so the combination (isCapsLock true, isShifted false) is a state the code can never produce yet... Impact: Medium. Effort: Medium. (`SleepTokenKeyboard/KeyboardRootView.swift:14-15`) — added 2026-07-26, completed 2026-07-26
- [x] **[Performance & Memory]** A shift tap re-evaluates all 26 letter keys, because each key carries a freshly allocated closure — Letter keys do not display shift state at all — case is resolved at tap time inside insertLetter — but isShifted is a parameter of LetterPage, so toggling shift or caps lock re-runs... Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:189`) — added 2026-07-26, completed 2026-07-26

### 9. Numbers and punctuation page (the "123" page)

_Current state: The data itself is correct and tidy: three rows of ten, all 30 symbols distinct, so the `id: \.self` identity the ForEach relies on can never collide. The page also reuses the letter page's keyHeight/rowGap metrics and the same BackspaceKeyButton, so it stays in step with the rest of the keyboard for free._

- [x] **[UI + Cleaner code + Architecture]** Every key on the 123 page is built from KeyChromeButton, so it inherits function-key styling and chrome-sized type by accident — SymbolsPage renders all 30 digit and punctuation keys with KeyChromeButton, the component written for the bottom function bar. Impact: Medium. Effort: Medium. (`SleepTokenKeyboard/KeyboardRootView.swift:232`) — added 2026-07-26, completed 2026-07-26
- [x] **[Cleaner code + Architecture + Performance & Memory + Resilience & Tests]** The symbol table is a private instance literal in the view, rebuilt per construction and enumerated only to discard the index — SymbolsPage owns its 30 symbols as an instance `private let` with a literal default inside a private struct in the extension target — so the arrays are re-materialised every time a... Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:215`) — added 2026-07-26, completed 2026-07-26

### 10. Keyboard chrome (globe/space/return/layout/style bar) and adaptive keyboard height

_Current state: The chrome plumbing is the right shape: the globe key correctly appears only when needsInputModeSwitchKey is true, the height constraint is installed at priority 999 so it never fights the system, rotation re-applies it via viewWillTransition, and an onNeedsHeightUpdate callback already exists as the view->controller channel. The mechanism is sound; what it computes is not._

- [x] **[UI + UX + Resilience & Tests + Architecture + Cleaner code]** Keyboard height is a hand-maintained table that under-allocates the shipped default: the "landscape" branch is taken in portrait, and the grid and 123 pages need a fourth row it never accounts for — preferredKeyboardHeight is six literal totals chosen from inputs the view does not use, while the real drivers (keyHeight 44/40, rowGap 6, bottomBarHeight 42, 8/4 padding, row counts) live... Impact: High. Effort: Medium. (`SleepTokenKeyboard/KeyboardViewController.swift:88`) — added 2026-07-26, completed 2026-07-26
- [x] **[UI + UX + Cleaner code]** Two adjacent chrome buttons both read "ABC" in the default configuration, the layout button ships the misspelled "QWRTY", and three neighbouring keys use three different label conventions — The key-style button shows the destination style, so with the default runeArt it reads 'ABC'; the page button also shows its destination, so on the 123 page it reads 'ABC' too. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:87`) — added 2026-07-26, completed 2026-07-26
- [x] **[Performance & Memory + Cleaner code + Architecture]** viewWillAppear and viewDidAppear have byte-identical bodies, so the root view is rebuilt three times per appearance with two forced layout passes — Both appearance callbacks execute the same three statements in the same order, with no comment explaining why, so neither call site is identifiable as authoritative and a maintainer cannot... Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardViewController.swift:16-21`) — added 2026-07-26, completed 2026-07-26
- [x] **[Accessibility]** KeyPressStyle animates a scale on every keypress without checking Reduce Motion — KeyPressStyle is the shared button style for every key in the extension — letters, shift, backspace, symbols and all chrome. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:368`) — added 2026-07-26, completed 2026-07-26
- [x] **[Performance & Memory + Security & Privacy]** The extension registers and ships a custom rune font it never draws, and compiles the persistent font-installer into the same process — The extension's Info.plist declares UIAppFonts with SleepTokenRunes.ttf and the 11KB face is bundled because project.yml lists the whole SleepTokenKeyboard directory as sources, so iOS... Impact: Low. Effort: Low. (`SleepTokenKeyboard/Info.plist:41-44`) — added 2026-07-26, completed 2026-07-26
- [x] **[UI]** No shared spacing scale: rows use 2/3/5/6pt insets and gaps, and function keys are 34/42/48/50/60pt wide — Horizontal insets are set independently at three nesting levels, so rows do not share a left or right edge: the root applies 3pt, the bottom bar adds another 2pt, the grid function row adds... Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:48`) — added 2026-07-26, completed 2026-07-26
- [x] **[UX]** The space bar is styled identically to the modifier keys and squeezed by five fixed widths — Every key in the bottom bar is the same KeyChromeButton with the same systemGray4 fill, so space and return carry no visual weight over the layout/style/123 modifiers — the bar reads as one... Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:103-106`) — added 2026-07-26, completed 2026-07-26
- [x] **[Cleaner code]** The height constraint uses the legacy NSLayoutConstraint initialiser four lines above the modern anchors it sits with — installKeyboardUI builds the height constraint with the seven-argument NSLayoutConstraint initialiser — including a force-unwrapped `view!`, an `attribute: .notAnAttribute` sentinel and a... Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardViewController.swift:46-54`) — added 2026-07-26, completed 2026-07-26
- [x] **[Accessibility]** Space and return are character-emitting keys but are opted out of `.isKeyboardKey` with the chrome — the July opt-out comment claims bottom-bar keys are "navigation actions", which is false for exactly space (inserts " " or ". ") and return (inserts "\n"); the two most-typed keys lose VoiceOver lift-to-type while every letter has it. Add an opt-in flag on ChromeKeyButton for those two and correct the comment. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:594-596,160-169,174-183`) — added 2026-07-31, completed 2026-07-31
- [x] **[UI]** In landscape the 42pt bottom bar towers over the 32pt key rows — compact key heights shrink but `bottomBarHeight` is a single constant, inverting the size hierarchy between typing keys and chrome. Derive a compact bottom-bar height alongside the compact key height in KeyboardMetrics. Impact: Low. Effort: Low. (`Shared/KeyboardMetrics.swift`, `SleepTokenKeyboard/KeyboardRootView.swift:92-93`) — added 2026-07-31, completed 2026-07-31
- [x] **[Architecture]** Height inputs travel two transports — page reaches the controller via the `onHeightInputsChanged` callback while layout mode arrives by re-reading `KeyboardPreferences` inside `preferredKeyboardHeight`; one value-carrying channel would make the height derivation testable end to end. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardViewController.swift:105-113`) — added 2026-07-31, completed 2026-07-31

### 18. Theme system: Ritual & Even in Arcadia

_Current state: Single observable store with stable raw values, palette that rethemes every call site untouched, deterministic petal layout, TimelineView correctly paused under Reduce Motion, ceremony hides the raw recolour behind an opaque curtain; persistence and hash determinism tested._

- [x] **[Accessibility]** Ceremony curtain is not modal to VoiceOver — covered controls stay reachable for the ~3.5 s it plays and the transition is never announced; `.accessibilityHidden(ceremonyActive)` on the content plus an announcement at `play()` start. Impact: Medium. Effort: Low. (`SleepTokenKB/ContentView.swift:115-125`, `SleepTokenKB/ArcadiaRevealView.swift:80-82`) — added 2026-07-26, completed 2026-07-26
- [x] **[Resilience & tests]** Ceremony cancellation fast-forwards through `try?` sleeps — under task cancellation the remaining timeline runs in one burst and `onCurtainClosed` can fire before the curtain is opaque; make cancellation explicit and commit the terminal state. Impact: Low. Effort: Low. (`SleepTokenKB/ArcadiaRevealView.swift:95-113`) — added 2026-07-26, completed 2026-07-26
- [x] **[Resilience & tests]** ThemeStore's fallback and palette switching asserted around, not through, the real code — the singleton's private init is untestable and no test flips the mode and asserts a colour changes; add an internal `init(defaults:)` and one palette-difference test. Impact: Low. Effort: Low. (`SleepTokenKB/Theme.swift:36-39,52`, `SleepTokenKBTests/ThemeTests.swift:46-48`) — added 2026-07-26, completed 2026-07-26
- [x] **[Performance]** PetalField recomputes frame-invariant seeds, colors, and paths every frame — hoist per-petal descriptors and draw via context transforms. Impact: Low (downgraded from Medium — the waste is microseconds against the 24fps budget). Effort: Low. (`SleepTokenKB/Theme.swift:318-348`) — added 2026-07-26, completed 2026-07-26
- [x] **[Cleaner code]** Arcadia rose and champagne literals hand-copied across Theme and the ceremony — three sites for rose, two for champagne; name them as fixed constants so the ceremony's stable-palette invariant holds by construction. Impact: Low (downgraded from Medium). Effort: Low. (`SleepTokenKB/Theme.swift:109,127,153`, `SleepTokenKB/ArcadiaRevealView.swift:26-27`) — added 2026-07-26, completed 2026-07-26
- [x] **[Consistency]** App-level `.tint(Theme.gold)` duplicates ContentView's tracked tint and lives where Observation tracking is not guaranteed — remove the app-level copy. Impact: Low. Effort: Low. (`SleepTokenKB/SleepTokenKBApp.swift:11`, `SleepTokenKB/ContentView.swift:114`) — added 2026-07-26, completed 2026-07-26

### 19. Latin read-back with live spell check

_Current state: latinTranslation is a small pure function with thorough round-trip coverage; the strip is visually polished with a two-channel flag treatment (color + underline) kept distinct from both themes' accents._

- [x] **[UX]** Lowercase-only checking false-flags proper nouns central to the app's vocabulary — "EUCLID" is flagged because only the lowercase form is checked while the strip displays uppercase; accept a word if lowercase or `.capitalized` passes. Impact: High. Effort: Low. (`SleepTokenKB/RunePadView.swift:123-128,163`, `Shared/Alphabet.swift:77-81`) — added 2026-07-26, completed 2026-07-26
- [x] **[UX]** The in-progress column flags mid-word, jittering the caption and re-fitting the canvas per keystroke — exclude the trailing column until it is finished (space or a later column), unlike the comment's claim of matching system behaviour. Impact: Medium. Effort: Low. (`SleepTokenKB/RunePadView.swift:118-131,148-152`) — added 2026-07-26, completed 2026-07-26
- [x] **[Accessibility]** VoiceOver never hears which words are flagged — the explicit "Reads …" label erases the spell-check signal entirely; add an accessibilityValue listing flagged words. Impact: Medium. Effort: Low. (`SleepTokenKB/RunePadView.swift:155-156,164-168`) — added 2026-07-26, completed 2026-07-26
- [ ] **[Performance]** UITextChecker rebuilt and every word re-checked on every body evaluation — including status/isExporting changes unrelated to text; hoist to `@State` updated in `.onChange(of: text)` with a small memo. Impact: Low (downgraded from Medium — millisecond-scale at this app's text sizes). Effort: Low. (`SleepTokenKB/RunePadView.swift:120-131,136`) — added 2026-07-26
- [x] **[Cleaner code]** latinWords recomputed four times per render and translationText is impure — bind once, pass explicitly, make the Text builder pure. Impact: Low. Effort: Low. (`SleepTokenKB/RunePadView.swift:135-156,160-174`) — added 2026-07-26, completed 2026-07-26
- [ ] **[Resilience & tests]** The flag pipeline above latinTranslation has zero coverage — columns split, empty-column filter, and the `count > 1` gate are view-private; extract the pure pipeline beside latinTranslation with an injectable checker. Impact: Low (downgraded from Medium — preventive, no observed defect). Effort: Medium. (`SleepTokenKB/RunePadView.swift:112-131,178-184`) — added 2026-07-26

### 21. Text-entry conveniences

_Current state: The four rules are pure, exhaustively unit-tested in isolation (46 tests), and the closure-based HostField correctly reads the proxy live rather than snapshotting. What the audit found is concentrated at the seams — the state the rules leave behind between calls, and the paths that skip re-derivation — not in the rules themselves._

- [x] **[UX + Resilience & tests + Cleaner code + Architecture]** Auto-armed shift carries no provenance: it ratchets, cannot be cancelled, and shift-tap escalates to caps lock — `nextShift` preserves any engaged state on the stated ground that "both engaged states are deliberate user acts", but the same function is what produces them, so an auto-armed `.shifted` is indistinguishable from a manual one. Consequences: deleting back into a word keeps shift up ("Hello. " → delete twice → type w → "HelloW" where the system gives "hellow"); an `.allCharacters` caps lock persists indefinitely; and tapping shift to cancel an auto-shift runs `toggled()`, so `.shifted → .capsLocked` — the one-tap cancel every iOS user expects instead engages ALL CAPS. Track provenance (an `autoArmed` flag or a state+origin pair), re-derive auto-armed states instead of preserving them, make shift-tap on auto-armed go to `.off`, and add the delete-into-word and tap-to-cancel sequences to AutocapitalizationTests. Impact: High. Effort: Medium. (`Shared/Autocapitalization.swift:19-23`, `SleepTokenKeyboard/KeyboardRootView.swift:229-238,243-249`, `Shared/ShiftState.swift:20-26`) — added 2026-07-31, completed 2026-07-31
- [x] **[UX + Resilience & tests + Architecture]** Shift and period state never reset when the host field changes; caps lock leaks between fields — `applyAutocapitalization` runs only on appear and after this keyboard's own keystrokes; `textDidChange` refreshes only when `returnKeyType` differs and `refreshRoot` cannot touch `@State` anyway. Focus an `.allCharacters` field then tap into a message body: `.capsLocked` survives and the user types ALL CAPS; after Messages clears its field on send, the next sentence starts lowercase; `lastSpaceAt` also survives, so a space in the new field within 1s of one in the old can inject ". ". The comment "the return key title is the only thing in the view that depends on which field is focused" was true before f7f4405 and is false now. Widen the field fingerprint to (returnKeyType, autocapitalizationType), pass a bumped `fieldGeneration` Int into the view, and `.onChange` reset shift, clear `lastSpaceAt`, and re-derive. Impact: High. Effort: Medium. (`SleepTokenKeyboard/KeyboardViewController.swift:114-133`, `SleepTokenKeyboard/KeyboardRootView.swift:28,31-33,189-196`) — added 2026-07-31, completed 2026-07-31
- [x] **[Resilience & tests]** RepeatingIconKeyButton never stops when the system cancels the touch: runaway delete — press tracking is `@State isPressed` driven only by `onChanged`/`onEnded`, and SwiftUI does not deliver `onEnded` on touch *cancellation* (incoming call banner, Notification Center pulled over the keyboard); the view stays visible so `onDisappear` never fires, and the repeater keeps deleting at 8–20 chars/sec while the user looks at the banner — the `guard !isPressed` even swallows the rescuing tap. Replace `@State` with `@GestureState` (reset runs on cancellation too) and drive start/stop from `.onChange` of it. Impact: High. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:551-586`) — added 2026-07-31, completed 2026-07-31
- [x] **[Accessibility]** Delete key rebuilt as a gesture-only view, losing guaranteed assistive-tech activation — the July delete was a Button (real accessibility activate action); today's `RepeatingIconKeyButton` is a bare Image with a DragGesture and only a label + `.isKeyboardKey` — no `.accessibilityAction`, no `.isButton` trait — so VoiceOver, Switch Control, and Full Keyboard Access have no guaranteed activation for the only delete on both pages. Add `.accessibilityAction { action(false) }` + `.isButton` (or an `.accessibilityRepresentation` Button). Impact: High. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:523-569`) — added 2026-07-31, completed 2026-07-31
- [x] **[Cleaner code + Architecture]** Autocapitalization is hand-placed at four call sites and the symbol path omits it — `applyAutocapitalization`'s comment claims it "runs after anything that moves the cursor", but SymbolsPage inserts via the bare `insert()` helper and never re-derives, so typing a digit or "." on the 123 page leaves shift stale. Share one post-mutation helper (or wire the symbols page like backspace already is) so the convention holds by construction. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:70,204-207,240-242`) — added 2026-07-31, completed 2026-07-31
- [x] **[Resilience & tests + UX]** lastSpaceAt survives intervening keystrokes, so the period shortcut fires on non-consecutive spaces — only a substitution clears it; letters, deletes, and symbol inserts leave it armed, so "type 'hi ', start a letter, delete it, tap space" within 1s emits "hi. " for a single space tap. The `Date()` arithmetic also goes negative on a clock shift. Reset it in `insert()` and `deleteBackward()`, and move to a monotonic clock. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:33,198-233`, `Shared/PeriodShortcut.swift:16-30`) — added 2026-07-31, completed 2026-07-31
- [x] **[UI]** Long adaptive return titles render edge-to-edge or shrunken in the fixed 56pt cap — ChromeKeyButton has no horizontal inset before the keycap fill, so "Continue" touches both cap edges and "Emergency" trips the 0.65 scale floor, rendering visibly smaller than "space" beside it. Add ~4pt inner padding and size the return frame for the longest common title. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:174-183,617-625`, `Shared/ReturnKeyTitle.swift:21-23`) — added 2026-07-31, completed 2026-07-31
- [x] **[Security & privacy]** Privacy manifests still document the pre-Full-Access posture and the retired App Group ID — the extension manifest's comments assert `RequestsOpenAccess` is false and Full Access is "never asked for" (both now false), and the host manifest names the retired `group.ai.altivum.SleepTokenFanKB`; PRIVACY.md directs readers to these files as the formal declaration, so the cited document contradicts the policy. Comment-block rewrite only; XML values are already correct. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/PrivacyInfo.xcprivacy:12-20`, `SleepTokenKB/PrivacyInfo.xcprivacy:17`) — added 2026-07-31, completed 2026-07-31
- [x] **[Cleaner code]** The delete key's 7-line construction is duplicated verbatim between LetterPage and SymbolsPage — same icon, fill, label, closure, and frame stated twice; extract one BackspaceKey. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:343-351,376-382`) — added 2026-07-31, completed 2026-07-31
- [x] **[Cleaner code + Architecture]** RepeatingIconKeyButton hand-copies KeyPressStyle's press appearance — the opacity/scale/reduce-motion/animation math exists twice and must stay in lockstep; extract a shared `pressAppearance(isPressed:)` modifier. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:540-547,632-645`) — added 2026-07-31, completed 2026-07-31
- [x] **[Cleaner code + Performance]** PeriodShortcut materializes the whole context into an Array to inspect two characters — `Array(contextBefore)` + `removeLast()` + O(n) `count` guard where `last` / `dropLast().last` answer directly; the space path also eagerly reads the full proxy context before the window check can short-circuit. Impact: Low. Effort: Low. (`Shared/PeriodShortcut.swift:21-29`, `SleepTokenKeyboard/KeyboardRootView.swift:211-215`) — added 2026-07-31, completed 2026-07-31
- [x] **[Cleaner code]** isAtSentenceStart's whitespace-only early return re-encodes a rule the fallthrough already implements — the `trimmingCharacters` check duplicates what the reversed-scan fallthrough concludes, and allocates a trimmed copy of the entire context per keystroke doing it. Impact: Low. Effort: Low. (`Shared/Autocapitalization.swift:44-60`) — added 2026-07-31, completed 2026-07-31
- [x] **[Cleaner code]** KeyRepeat.interval's doc is off by one from how the loop uses it, and "the floor" describes a curve that does not exist — the comment says "the wait before the count-th repeat, counting from 1" but the loop sleeps *after* firing; `fastInterval` is documented as a floor under a curve that is actually a two-step function. Comment fix. Impact: Low. Effort: Low. (`Shared/KeyRepeat.swift:20-28`, `SleepTokenKeyboard/KeyboardRootView.swift:576-584`) — added 2026-07-31, completed 2026-07-31
- [x] **[Cleaner code]** loadPreferences() now performs three jobs its name does not admit — preference loading, height reporting, and autocapitalization seeding; rename or split so the appear-time contract is explicit. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:189-196`) — added 2026-07-31, completed 2026-07-31
- [x] **[UX]** Sentence and word boundaries ignore closing quotes and brackets — `He said "stop." ` does not arm shift because the scan looks only at the last non-whitespace character; skip trailing closers before testing for a terminator. Impact: Low. Effort: Low. (`Shared/Autocapitalization.swift:52-59`) — added 2026-07-31, completed 2026-07-31
- [x] **[UX]** Return key ignores enablesReturnKeyAutomatically — where the system keyboard disables return in an empty search field, this one stays active; read the proxy's flag and dim/disable accordingly. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:174-183`, `SleepTokenKeyboard/HostField.swift`) — added 2026-07-31, completed 2026-07-31
- [x] **[Security & privacy + Performance]** Autocapitalization reads the host document even when the decision cannot depend on it — both proxy reads run for `.none` fields and engaged-shift early returns, including at 20Hz during a held delete; order the checks so context is read only when the rule needs it. Least-privilege hygiene, not a leak — nothing retains the text. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:243-249`, `Shared/Autocapitalization.swift:14-36`) — added 2026-07-31, completed 2026-07-31
- [x] **[Performance]** lastSpaceAt is render-irrelevant bookkeeping held in @State — every space press invalidates the whole keyboard view tree for a value nothing renders; move it to a non-observed holder. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:31-33`) — added 2026-07-31, completed 2026-07-31
- [x] **[Performance]** Return-key type is read through the proxy twice per body evaluation and lastReturnKeyType is never seeded — the title and accessibility label each call `host.returnKeyType()`, and the unseeded controller cache guarantees one redundant full rebuild per presentation; bind once per body, seed on install. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:174-183`, `SleepTokenKeyboard/KeyboardViewController.swift:24-27,120-126`) — added 2026-07-31, completed 2026-07-31
- [x] **[Performance]** Taptic engine is warmed on appear without the Full Access gate every fire enforces — `prepare()` spins the engine for users who can never feel it; gate the warm-up on `host.hasFullAccess()` like `haptic()` already does. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:191-192,251-258`) — added 2026-07-31, completed 2026-07-31
- [x] **[UX + Resilience & tests]** Cancelling caps lock in an `.allCharacters` field lasts exactly one letter — `userTappedShift` yields `.off` with no provenance, so the next `applyAutocapitalization` re-derives `.capsLocked` and the user is silently re-locked after one lowercase character; sustained lowercase is impossible, and no AutoShiftTests sequence pins the behaviour. Extend provenance with a manual-off value (or remember the declined auto-caps until the `fieldGeneration` reset) and add the cancel-then-type sequence. Beyond this, AutoShift and SpaceTracker implement the 2026-07-31 prescriptions faithfully. Impact: Medium. Effort: Low. (`Shared/AutoShift.swift:36-43`, `Shared/Autocapitalization.swift:43-44`) — added 2026-08-03, completed 2026-08-03 (a `capsDeclined` flag: any tap that turns the lock off outlives re-derivation for the rest of the field, withdrawn by `setCapsLock`)
- [x] **[Accessibility]** Letter keys always announce the uppercase letter, so AutoShift's case is inaudible — the July keycap-case fix was visual-only: `.accessibilityLabel(letter.upperLatin)` is unconditional and the rune-art face pins `shifted` to false, so VoiceOver users get no cue when autocapitalization silently arms or releases. Give the label the real case: `letter.latin` lowercase, a "capital" cue uppercase, matching stock phrasing. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:1192,673-675`) — added 2026-08-03, completed 2026-08-03
- [x] **[Cleaner code]** `didInsertLetter`'s spec is welded onto `setCapsLock`'s doc comment — lines 45-46 describe one-shot consumption, which `setCapsLock` never performs, while `didInsertLetter` itself is undocumented; in this codebase rule-file doc comments are the behavioural spec, so a spec on the wrong function is a live trap. Move the two sentences onto their function. Impact: Low. Effort: Low. (`Shared/AutoShift.swift:45-52,57`) — added 2026-08-03, completed 2026-08-03 (folded into the shift-semantics batch: the rewrite passed through those lines)

### 25. Glide typing (slide-to-type)

_Current state: The decode core is genuinely clean — pure value types throughout, geometry held to LetterPage by a parity test, a deterministic decoder with an explicit budget contract, cancellation designed in and tested at the rule level. Every finding sits at the seams: load timing, view invalidation, and the mutation paths around the committed word._

- [x] **[Performance]** Supplementary-lexicon merge can run the 50k-word parse on the main thread during keyboard presentation — `viewDidLoad`'s completion hops to main and touches `GlideLexicon.shared` before the utility-priority warm (scheduled later, from `onAppear`) has finished, so a cold extension launch can block the main thread for the ~100-400ms parse exactly while the keyboard slides up, with glide on by default. Route the merge through the same off-main once-parse and start the warm from `viewDidLoad`. Impact: High. Effort: Low. (`SleepTokenKeyboard/KeyboardViewController.swift:63-68`, `Shared/GlideLexicon.swift:30-42`) — added 2026-08-03, completed 2026-08-03
- [ ] **[UI + UX + Performance]** The trail never evaporates: the whole word's path persists at a 0.15 opacity floor and per-frame stroke work grows with trace length — every segment from the first sample is stroked on every redraw, webbing long words over the keys still being aimed at, where stock's comet fades behind the finger. Render only a trailing window (last ~40 points, opacity reaching zero at the tail) while the decoder keeps the full trace; the reduce-motion uniform line can keep the full path. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:798-808`, `Shared/GlideSession.swift:19-24`) — added 2026-08-03
- [ ] **[Performance]** Every glide sample re-evaluates the whole 30-key LetterPage, not just the trail — `GlideSession` lives in LetterPage's `@State` and body reads `glide.points`, so each `extend()` (60-120Hz) re-runs the GeometryReader, re-diffs the rows, and reconstructs 26 key values with fresh closures at the moment latency matters most. Hold live points in a reference-type box only GlideTrailView observes, per the spaceTracker "nothing renders it" precedent. Impact: Medium. Effort: Medium. (`SleepTokenKeyboard/KeyboardRootView.swift:641,712-717,750`) — added 2026-08-03
- [ ] **[Architecture + Resilience & tests]** `replaceCurrentWord` bypasses the file's mutation discipline, leaving GlideUndo armed with the replaced word's stale length — it calls the raw proxy closures and notes only spaceTracker, so accepting a glide alternate leaves a window where backspace whole-word-deletes the wrong count; the eventual disarm happens only because a second omission makes its own echo look external. Route it through the same interrupts `insert()` performs and pin glide-commit → accept-alternate → backspace in GlideCommitTests. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:569-575,417-423`) — added 2026-08-03
- [ ] **[Resilience & tests]** The extension's lexicon load fails silent-empty and no test guards the appex's copy of glide_lexicon.txt — a missing resource yields an empty lexicon with no diagnostic, every glide then commits corner-letter garbage via the fallback, and the 45k-word tests resolve `Bundle(for:)` to the test host, so deleting the one `project.yml` line that embeds the file stays green while glide is destroyed on device. Add a bundle-integrity test against the embedded appex (the rune-font precedent) and log once on the empty fallback. Impact: Medium. Effort: Low. (`Shared/GlideLexicon.swift:30-42`, `project.yml:99`) — added 2026-08-03
- [ ] **[Security & privacy]** PRIVACY.md and the extension manifest comments predate glide, emoji recents, and the suggestion bar — the stored-preferences table omits emoji recents (a typed-emoji history) and the glide toggle, "keeps no history" now sits beside exactly such a record, and no privacy document discloses the supplementary-lexicon read README.md openly admits; the XML values remain correct, the drift is entirely in the prose the documents tell readers to verify against. Prose-only pass, same shape as the completed 2026-07-31 manifest rewrite. Impact: Medium. Effort: Low. (`PRIVACY.md:45-67`, `SleepTokenKeyboard/PrivacyInfo.xcprivacy:21-24`) — added 2026-08-03
- [ ] **[Architecture]** KeyboardRootView.swift is 1400 lines with five self-contained views inline — GlideTrailView, SuggestionBar, SymbolsPage, EmojiPage, GlobeKey and the whole keycap chassis take plain values and closures with zero root-state reads, i.e. already-clean seams; the RunePadView split is the recorded precedent, and the extension target is a directory reference so new files cost no project edits. Split along the MARK boundaries. Impact: Medium. Effort: Medium. (`SleepTokenKeyboard/KeyboardRootView.swift:786-1400`, `project.yml:78`) — added 2026-08-03. Note 2026-08-03: EmojiPage extracted first (the shift-semantics batch pushed the file past the 1450 lint ceiling); the file is at 1365 and the remaining seams still want their own files.
- [ ] **[Resilience & tests]** A field switch mid-gesture never reaches the glide session — the per-field reset block cannot touch LetterPage's `@State`, so an in-flight trace survives focus moves and commits its word into the NEW field; `GlideSession.cancel()` exists and is tested for exactly this discard. Pass `fieldGeneration` into LetterPage and cancel on change. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:194-204,641`, `Shared/GlideSession.swift:34`) — added 2026-08-03
- [ ] **[Security & privacy]** The supplementary lexicon (contact names, text replacements) is requested even when glide typing is off — the `glideTypingEnabled` guard sits inside the completion, after iOS has delivered the words into the process, and glide is their only consumer. Guard before `requestSupplementaryLexicon`; the delivery-time guard stays as a second gate. Least-privilege hygiene, not a leak. Impact: Low (downgraded from Medium — matches the doc's own rating of the same class at line 230). Effort: Low. (`SleepTokenKeyboard/KeyboardViewController.swift:63-65`) — added 2026-08-03
- [ ] **[Security & privacy + Performance]** `GlideCommit.insertion` reads the host document before the auto-space rule can need it — `contextBefore` is a plain `String?` evaluated eagerly though it is irrelevant whenever `lastInsertWasGlide` is false (the common case); the repo's convention, established in `Autocapitalization.nextShift` and pinned by its tests, is an `@autoclosure` that defers the proxy read. Impact: Low. Effort: Low. (`Shared/GlideCommit.swift:32-36`, `SleepTokenKeyboard/KeyboardRootView.swift:465-467`) — added 2026-08-03
- [ ] **[Performance]** `candidates()` recomputes the end-key distance per lexicon entry instead of qualifying end letters once — thousands of dictionary lookups plus `hypot` calls per decode for what is a property of the 26 centers; mirror the existing `startLetters` line with a precomputed `endLetters` set. Impact: Low. Effort: Low. (`Shared/GlideLexicon.swift:96-102`) — added 2026-08-03
- [ ] **[Performance & Memory]** The lexicon retains a second 50k-word Set for the process lifetime to serve init dedupe and test-only APIs — ~1-1.5MB held forever in an extension with a tight memory ceiling; `containsWord`/`mostFrequentWords` have no production callers (verified by grep). Make the dedupe set transient inside init and reimplement the test hooks as bucket scans. Impact: Low. Effort: Low. (`Shared/GlideLexicon.swift:25,60-65,79-86`) — added 2026-08-03
- [ ] **[Cleaner code + Architecture]** The local-echo discipline exists in three copies — GlideUndo reimplements SpaceTracker's `pendingLocalEcho` trio line for line (its own doc admits it), and `showsGlideAlternates` hand-rolls a third inside the onChange; the coalescing caveat is documented on one copy only. Extract one `LocalEchoGate` in Shared/ that all three compose, carrying the caveat in one place. Impact: Low. Effort: Low. (`Shared/GlideCommit.swift:41-79`, `Shared/SpaceTracker.swift:12-47`, `SleepTokenKeyboard/KeyboardRootView.swift:62-64,212-217`) — added 2026-08-03
- [ ] **[Accessibility + Consistency]** The VoiceOver gate on glide is read non-reactively — `UIAccessibility.isVoiceOverRunning` at body time with no change subscription, so the drag gesture stays armed after VoiceOver turns on until an unrelated re-render, while the same view already takes Reduce Motion from the environment. Use `@Environment(\.accessibilityVoiceOverEnabled)`. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:732-734,718`) — added 2026-08-03

### 26. Suggestion bar with spelling correction

_Current state: The engine's conservatism is exactly right — the literal always kept, minimum length, digits exempt, rejections sticky — and it is well tested at the rule level. All three Highs sit in the wiring between the engine and the document: where corrections fire, where they must not, and who is told._

- [x] **[UX]** Correction sees only text left of the caret, so a mid-word caret triggers suggestions and auto-replacement for a fragment — `HostField` vends no context-after closure at all, so placing the caret after "wor" in "worlds" and pressing space splices the replacement into the middle of the word ("worklds "); stock stands down whenever the next character is a letter. Wire `documentContextAfterInput` through HostField and guard both `refreshSuggestions` and `autoCorrectFinishedWord`; pin the mid-word sequences as pure-rule tests. Impact: High. Effort: Medium. (`SleepTokenKeyboard/KeyboardRootView.swift:561,598-604`, `SleepTokenKeyboard/HostField.swift:9-11`) — added 2026-08-03, completed 2026-08-03
- [x] **[UX + Resilience & tests]** Auto-replacement fires only on the space bar — return and punctuation ship the typo: "teh" + Send in Messages ships "teh", and "teh." via the 123 page stays "teh." — the two most common ways a message's last word ends. Call `autoCorrectFinishedWord()` from the return action and from the symbols path when the inserted symbol is a `WordBoundary` boundary; the engine's guards already carry everything needed. Impact: High. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:427-431,297-300,137-142`) — added 2026-08-03, completed 2026-08-03
- [x] **[Accessibility]** Corrections are completely silent to VoiceOver — auto-replacement rewrites the document and candidates change with no announcement anywhere in the extension (the sole UIAccessibility reference is the glide gate), so a blind user's "teh " becomes "the" with zero audible indication; stock speaks autocorrections. Announce replacements (and optionally first-appearing candidates) via a pure helper whose wording SuggestionBehaviourTests can pin. Impact: High. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:598-604,556-564`) — added 2026-08-03, completed 2026-08-03
- [ ] **[Resilience & tests]** Multi-scalar emoji glue to the current word — `isBoundary` requires EVERY scalar in the boundary set, but VS16 (Mn) and ZWJ (Cf) scalars are in none, so "❤️" and "😮‍💨" — both shipped by this keyboard's own catalog — read as word characters and `currentWord("hi ❤️hey")` returns "❤️hey"; correction goes silent or wrong when a typo abuts one. Change `allSatisfy` to `contains(where:)` (é and combining marks stay in-word) and add emoji boundary tests. Impact: Medium (downgraded from High — the worst path hinges on unverified UITextChecker behaviour over emoji-bearing ranges). Effort: Low. (`Shared/Suggestions.swift:13-22`) — added 2026-08-03
- [ ] **[Resilience & tests]** The guess cache is keyed on the lowercased word but stores case-sensitive results — whichever casing is computed first wins the session, so a cached sentence-start "Teh" → ["The"] makes a later mid-sentence "teh" + space silently produce a wrong capital, applied without user review. Key by exact word, or normalise the cached casing on read. Impact: Medium. Effort: Low. (`Shared/Suggestions.swift:104-124`) — added 2026-08-03
- [ ] **[Performance]** `UITextChecker.guesses` runs synchronously on the main actor for nearly every keystroke mid-word — every keystroke is a fresh prefix so the per-word memo misses by construction, and the expensive call (single-digit to tens of ms) lands inside the SwiftUI update in the same window as the controller's root rebuild. Compute bar candidates on a background executor with a generation token; the boundary path stays synchronous (already memoised by the bar's last refresh). Impact: Medium. Effort: Medium. (`Shared/Suggestions.swift:103-126`, `SleepTokenKeyboard/KeyboardRootView.swift:556-564`) — added 2026-08-03
- [ ] **[UI]** Slot widths jump per keystroke because the bar divides by however many candidates exist — 2, 3, or 4 equal-share slots as checker results change, so the literal's edges and the dividers slide between renders where stock's three-slot geometry never moves. Fix the column count (three for spelling, four for a glide round) and render unoccupied columns blank. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:827-839,862`) — added 2026-08-03
- [ ] **[UX]** Accepting a bar candidate does not commit the trailing space stock muscle memory expects — the caret lands flush against the replacement and the next keystroke continues the same word; stock commits candidate-plus-space and swallows that space before punctuation. Add the space WITH the punctuation-swallow compensation, or not at all — half of stock's behaviour would be worse than none. Glide alternates stay space-free (GlideCommit's consecutive-glide rule owns that seam). Impact: Medium. Effort: Medium. (`SleepTokenKeyboard/KeyboardRootView.swift:578-583,569-575`) — added 2026-08-03
- [ ] **[Accessibility]** Candidate text is fixed at 16pt while the chassis scales every key face with `@ScaledMetric` — at accessibility type sizes the keycaps grow while the exact words a low-vision user must read to accept or decline a correction stay small (and can shrink to ~11pt via the scale factor). Add a fourth scaled metric at the root and pass it in the way the pages take faceSize. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:858,88-90`) — added 2026-08-03
- [ ] **[Performance]** The guess-cache wipe-at-512 discards the hot entries and `keepAsTyped`'s cache write is dead — the wipe drops the current word's prefixes mid-typing so the next keystroke re-pays a `guesses()` call just memoised, and the kept-word guard returns before the cache is consulted so its write can never be read. Delete the dead write; soften the wipe to spare the in-progress word. Impact: Low. Effort: Low. (`Shared/Suggestions.swift:123,98,105-108`) — added 2026-08-03

### 27. Emoji page

_Current state: The data layer is tidy and tested — ordered categories, a bounded no-duplicate recents rule, stock's clock-tab convention. The findings all live in the page chrome, and one interaction defect undermines the page's own entrance._

- [x] **[UI + Accessibility]** The category strip overflows every iPhone; the last tabs hide behind an indicator-less scroll — ten fixed 38pt tabs need 398pt where the strip gets 320-385pt, so Symbols and Flags are entirely off-screen on smaller phones with no partial-tab peek and no indicator; stock's strip never scrolls. Share the width in a plain HStack so all ten always show (~32-38pt each — stock's own tab size); the accessibility lens independently flagged the 38pt targets against the 44pt floor, but stock itself accepts sub-44 here and the full-height target stays. Impact: High. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:988-1010`) — added 2026-08-03, completed 2026-08-03
- [ ] **[UX]** Long-pressing the emoji key for the options panel also toggles the page underneath it — SwiftUI Buttons fire on touch-up regardless of press duration and `simultaneousGesture` does not claim the touch, so lifting after the 0.4s panel opens still flips letters/emoji behind the modal; after Done the user is on a page they never chose. One-line guard on `showsKeyboardOptions` in the action. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:253-268`) — added 2026-08-03
- [ ] **[UX]** Switching categories keeps the previous category's scroll offset — the ScrollView's identity persists across the entry swap, so tapping Food while deep in Smileys lands mid-list (or clamped); stock always presents a category from its start. Attach `.id(category)` to the ScrollView. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:961-984`) — added 2026-08-03
- [ ] **[Cleaner code + UX + Consistency]** Every emoji tap fires two haptic impacts — `insertEmoji` calls `haptic()` and then `insert()`, whose first statement is `haptic()` again; emoji is the only key on the keyboard with doubled feedback, the deviation is uncommented, and four lenses found it independently. Delete the extra call — `insert()` owns keystroke feedback. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:543-546,417-418`) — added 2026-08-03
- [ ] **[UI]** The category strip stays 44pt in landscape, towering over 32pt keys — `stripHeight` has no compact variant while every other height on the page adapts; the same hierarchy inversion the feature-10 pass fixed for the bottom bar. Add `emojiStripHeight(compact:)` beside `bottomBarHeight` in KeyboardMetrics. Impact: Low (downgraded from Medium — the grid regains ~10pt in a minority orientation and scrolls anyway). Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:938,1010-1012`, `Shared/KeyboardMetrics.swift:27-29`) — added 2026-08-03
- [ ] **[Resilience & tests]** `emojiRecents` persistence has zero tests and is invisible to the preference harness — a hand-rolled stringArray path with a private key `trackedKeys` cannot snapshot, written on every emoji insert; the only untested accessor in the file, and a future test would leak state into the developer's suite. Make the key public, track it, round-trip it. Impact: Low. Effort: Low. (`Shared/LayoutMode.swift:136-149`, `SleepTokenKBTests/KeyboardPreferencesTests.swift:19-25`) — added 2026-08-03

### 28. Stock-parity page and shift gestures

_Current state: Both rules are faithful to stock, take time as a parameter per the house convention, and are tested as value sequences. Both Mediums are wiring omissions in the view — the rules' own doc comments promise interrupts the wiring never delivers._

- [x] **[Resilience & tests + Consistency]** The pending shift tap survives intervening keystrokes and external text changes — `capsLockTap.interrupt()` is wired only to field changes, though the type's doc promises abandonment on external text changes too, so shift → letter → shift inside 0.35s latches caps lock during fast typing of two capitalized words. Interrupt from `insert()`/`deleteBackward()` and the hostTextGeneration onChange — the exact SpaceTracker pattern three lines above — and pin tap/interrupt/tap-inside-window in CapsLockTapTests. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:200,205-218,417-423`, `Shared/CapsLockTap.swift:34-37`) — added 2026-08-03, completed 2026-08-03
- [x] **[UX]** A slow second tap on a manually shifted key engages caps lock instead of releasing shift — past the 0.35s window the tap falls to `toggled()` (.shifted → .capsLocked), but stock's shift is a toggle at any speed, and CapsLockTap now makes the legacy cycle's middle step redundant; the 2026-07-31 fix covered auto-armed shift only. Send a manual .shifted to .off, preserving an assistive route to caps lock (keep the cycle under VoiceOver, or add an accessibility action); update AutoShiftTests:56-66, which pins the cycle. Impact: Medium. Effort: Low. (`SleepTokenKeyboard/KeyboardRootView.swift:526-538`, `Shared/AutoShift.swift:36-43`, `Shared/ShiftState.swift:20-26`) — added 2026-08-03, completed 2026-08-03 (assistive route: a named "Caps lock" accessibility action on the shift key; `ShiftState.toggled()` deleted with the cycle)
- [ ] **[Architecture]** FieldFingerprint's "identical traits mean identical derivation rules" claim went stale — `keyboardType` now drives two rules (`usesNumberPad`, `correctionAllowed`) but is not fingerprinted, so two fields differing only there are one field to the reset channel; mostly self-healing today, a silent gap for the next trait-dependent rule. Add `keyboardType` to the fingerprint. Impact: Low. Effort: Low. (`SleepTokenKeyboard/KeyboardViewController.swift:22-29,157-178`) — added 2026-08-03
- [ ] **[Cleaner code]** `rowCount`'s doc still claims the symbols pages add a dedicated row; the body says the opposite three lines down — half-stale since the inline-delete rework (true for grid, false for symbols), and PageParityTests pins the new shape. Rewrite the two doc lines. Impact: Low. Effort: Low. (`Shared/KeyboardMetrics.swift:96-103`) — added 2026-08-03

### Highest-impact refinement — 2026-08-03

Feature 26's correction-scope cluster. One boundary blindness — the pipeline knows only the text LEFT of the caret and only ONE word-ending key — produces the run's two worst behaviours: mid-word splices that corrupt text the user already typed ("worklds"), and the feature silently absent at the two most common word endings (return and punctuation). Add the VoiceOver silence and all three Highs on feature 26 are the same shape: the engine is right, the seams to the document are wrong. Wiring contextAfter through HostField, adding the two missing call sites, keying the cache by exact word, and announcing replacements closes three Highs and two Mediums in one pass over the same few functions — and makes every one of those sequences expressible as a pure-rule test.

### Suggested order — 2026-08-03

1. Feature 26 — the correction boundary pass: contextAfter through HostField with the mid-word stand-down, correction on return and boundary punctuation, the emoji boundary rule, the case-keyed cache, and the VoiceOver announcement. Three Highs, two Mediums, one seam.
2. Feature 25 — the `viewDidLoad` lexicon pass: off-main once-parse plus glide-enabled gating before the request (the performance High and the privacy Low are the same six lines), with the PRIVACY.md/manifest prose pass that documents what actually ships.
3. Feature 27 — the emoji interaction batch: strip overflow, long-press page flip, per-category scroll reset, double haptic. All small, all in EmojiPage and the chrome row.
4. Features 28 + 21 — the shift semantics batch: CapsLockTap consecutiveness wiring, the slow-second-tap-releases decision, manual-off provenance for `.allCharacters` fields, and the spoken letter case. One design conversation, then four small fixes.
5. Feature 26 — bar polish: fixed slot geometry, the scaled candidate metric, async guesses, and the trailing-space decision (with its punctuation compensation, or not at all).
6. Feature 25 — glide session hygiene: replaceCurrentWord discipline, mid-gesture field-switch cancel, the appex lexicon bundle test, the evaporating trail, and the observable points box.
7. The architecture pass — split KeyboardRootView along its MARK seams (the RunePadView precedent), extracting LocalEchoGate on the way.
8. The remaining Lows, grouped by file: Suggestions.swift cache hygiene, GlideLexicon memory + prune, GlideCommit's autoclosure, KeyboardMetrics strip height + rowCount doc, FieldFingerprint keyboardType, emojiRecents tests, the AutoShift doc move, and the reactive VoiceOver gate.

### Highest-impact refinement — 2026-07-31

Feature 21's shift-provenance cluster. One modelling gap — "auto-armed shift is indistinguishable from a manual shift" — produces the run's three worst user-visible behaviours: the ratchet (delete back into a word, next letter wrongly uppercase), the impossible cancel (shift-tap at a sentence start engages caps lock instead of releasing), and the cross-field caps-lock leak (via the missing field-change reset it compounds with). The 46 unit tests all pass because every test hands the rule a `current` state whose origin the test knows; the production caller is precisely the one that has lost that information. Fixing provenance plus the field-generation channel closes five of the seven High/Medium findings on feature 21 in one design move, and both fixes make previously untestable sequences (delete-into-word, tap-to-cancel, field-switch) expressible as unit tests.

### Suggested order — 2026-07-31

1. Feature 21 — shift provenance (+ symbol-path re-derivation folded in): the one design change, with the new AutocapitalizationTests sequences.
2. Feature 21 — field-generation channel from `textDidChange`: resets shift and `lastSpaceAt` per field; fixes the caps leak and the cross-field period misfire.
3. Feature 21 — `@GestureState` for the delete repeater (runaway on touch cancellation) and the `.accessibilityAction` restoration; feature 4's Rune Pad repeat retrofit (overshoot, cleanup, shared KeyRepeat) is the same pattern applied in the host app — do the three together.
4. Feature 21 — `lastSpaceAt` consecutiveness + monotonic clock.
5. Mediums in one styling/copy pass: return-cap padding and width, privacy-manifest comment rewrite, walkthrough step IV copy, caption ink tier, space/return `.isKeyboardKey`, shifted keycap case on ABC/hint faces.
6. The Low batch, grouped by file: KeyboardRootView cleanups (BackspaceKey extraction, pressAppearance modifier, loadPreferences split, return-key single read + seeded cache, lastSpaceAt out of @State, gated warm-up), Shared rule polish (PeriodShortcut string idioms, sentence-start simplification, quote/bracket boundaries, KeyRepeat doc fix), and the two feature-10 items.

### Highest-impact refinement — 2026-07-26

Feature 10 — \"Keyboard height is a hand-maintained table that under-allocates the shipped default.\" It is the only finding in the audit that breaks layout correctness rather than polish, and verification made it worse than originally filed: `KeyboardViewController.swift:88` derives `isLandscape` from the keyboard's own `view.bounds` (`bounds.width > bounds.height`), and a keyboard input view is wider than tall in portrait too — so after the first layout pass the flat 190/210 branch at :95-97 is taken in portrait as well, and the mode-aware table at :98-102 is effectively dead code. The A-Z grid layout and the 123 page each render a fourth full-height row (KeyboardRootView.swift:201-210 and :239-243), giving 238pt of fixed, non-compressible content inside a 190pt container that is bottom-aligned (:51), which pushes an entire key row outside the input view. Fixing it means replacing the literal table with a derivation from constants the view already owns — which simultaneously closes the keyFaceStyle drift, the missing height callback on the style button (:91-93), the invisible `showSymbols` page shape, and the ~88pt portrait dead band, and yields a pure function the existing test target can assert on.

### Suggested order — 2026-07-26

1. 1. Feature 10 — extract KeyboardMetrics + contentHeight() into Shared/ and derive preferredKeyboardHeight from it; fix the bounds-based orientation test; add onNeedsHeightUpdate to the style button; pass the current page up. This is the one correctness defect, and the shared-metrics extraction is the foundation several later items sit on.
2. 2. Feature 8 — replace the two shift Bools with a ShiftState enum in Shared/, pass it through LetterPage into ShiftKeyButton, and render/announce the three states (shift / shift.fill / capslock.fill, accessibilityValue, .isSelected). One piece of work: the enum refactor is the vehicle for the High-impact accessibility fix. Add ShiftStateTests in the same pass.
3. 3. Feature 6 — add .accessibilityAddTraits(.isKeyboardKey) to letter, shift, backspace and symbol keys (opting the chrome buttons out). Independent, Low effort, High impact for VoiceOver users.
4. 4. Feature 7 — dark-mode key palette: a KeyPalette dynamic color for the keycap fill at :287, lighten the field at :52 and KeyboardViewController.swift:12, gate or drop the shadow at :288. Isolated, Low effort, fixes legibility of every primary key in dark mode.
5. 5. Features 6 + 9 — extract the keycap chassis into one ViewModifier / IconKeyButton, then give SymbolsPage its own SymbolKeyButton with a content fill and explicit font size. Do these together; the extraction is a prerequisite for the 123-page restyle.
6. 6. Feature 7 — accessibilityHidden(true) on the nested SymbolGlyphView at :263 (and the hint Text at :268) so both key-face styles announce identically. Small and independent.
7. 7. Feature 10 — chrome labels: break the duplicate "ABC" tie, fix "QWRTY", and unify the three label conventions using LayoutMode.accessibilityLabel and KeyFaceStyle.next.title.
8. 8. Feature 6 — GeometryReader key unit in LetterPage and the derived home-row inset, replacing the literal 12. Do after the metrics extraction so the constants come from one place.
9. 9. Performance batch (features 6, 7, 8, 9, 10) — static let defaults in LayoutMode.swift:62, static impact generator, .equatable() on LetterKeyButton, static symbol rows + dropped enumeration, hoisted asset probe in SymbolGlyphView, drop the unused TTF and RuneFont from the extension target. All Low effort, all behaviour-preserving, all verifiable together.
10. 10. Lifecycle and hygiene batch (features 7, 10) — syncForAppearance(), guarded applyHeight, drop refreshRoot() from onNeedsHeightUpdate, delete showNextKeyboardKey, switch to the heightAnchor constraint, honour Reduce Motion, fix the press-in animation timing, resolve the App Group write path, and settle the single preference read path.
11. 11. Remaining Low cleanups (features 6, 7, 9, 10) — spacing/width constants, space-bar hierarchy, rune keycap centring, stale GeometricSymbol comments and dead diamond Path, row-index intent expressions, and the KeyboardLayout / symbol-table validation tests.


## Implementation notes — 2026-07-26

All 32 refinements for features 6-10 were implemented in the suggested order.

**New shared files**
- `Shared/KeyboardMetrics.swift` — one source of truth for keyboard geometry. Both the
  SwiftUI content and `KeyboardViewController` now derive height from `contentHeight(...)`,
  so the container can no longer promise less space than the rows occupy.
- `Shared/ShiftState.swift` — the three shift states as one value type; the illegal fourth
  combination of the old two-Bool encoding is now unrepresentable.
- `SleepTokenKeyboard/KeyPalette.swift` — dynamic key colours that keep primary keys the
  lightest surface in both appearances.

**Adapted rather than applied literally**
- *Landscape height*: rather than clamping the total (which is what allowed the overflow),
  `KeyboardMetrics` exposes compact key heights and both the view and the controller derive
  `compact` from the vertical size class, so they cannot disagree.
- *Reserved height*: the controller reserves `maxContentHeight` (tallest key style for the
  current page) rather than the exact current height, so toggling rune/ABC mid-session never
  clips a visible row.
- *Shared key unit*: applied to QWERTY only, where the column misalignment actually was.
  The grid layout keeps flexible key widths so its 9- and 8-key rows still fill the row.
- *`.renderingMode(.template)`*: removed only after verifying all 26 imagesets in **both**
  catalogs declare `template-rendering-intent`.

**Verification**
- `./scripts/test.sh` — builds both targets against the iOS Simulator 26.5 SDK and runs the
  full suite: **33 tests, 0 failures, 0 source warnings**.
- The script sets `DEVELOPER_DIR` because this machine's `xcode-select` points at
  CommandLineTools, which makes plain `xcodebuild` refuse to run even though Xcode is
  installed. That is why an earlier pass wrongly believed no iOS SDK was available.
- Two deprecations the build surfaced were fixed: `traitCollectionDidChange` →
  `registerForTraitChanges`, and `UIScreen.main.scale` → `@Environment(\.displayScale)`
  in Rune Pad.

**Still unverified**
- Everything device-specific: real VoiceOver behaviour, dark-mode contrast on hardware, and
  the actual keyboard height in a live text field. The simulator builds and the arithmetic
  is unit-tested, but neither proves the keyboard feels right in Messages.
- The haptic question raised under feature 6 (whether `impactOccurred` does anything with
  `RequestsOpenAccess = false`) still needs a real device to answer. The generator is now
  process-scoped and re-armed after each fire, but if it turns out to be inert on device,
  the honest follow-up is to remove it and stop presenting the haptics toggle as active.

## Update — 2026-07-30

The open haptic question was answered on a physical device: `impactOccurred` was
indeed inert with `RequestsOpenAccess = false`. Resolved by requesting Full Access
(a product decision, taken deliberately) with `haptic()` gated on `hasFullAccess`,
and every claim that asserted the old posture — PRIVACY.md, README, RELEASE.md
review notes, in-app copy, TestFlight description — was rewritten to match.
Features 21–24 were added to the inventory (text-entry conveniences, release
pipeline with the `status` stage, CI workflows, OG image generation); the test
suite stands at 121. Build 24 shipped to TestFlight the same day: beta review
auto-approved, live to the Fan Community group.
